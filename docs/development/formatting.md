# Formatting

Formatting is handled by Prettier. The npm scripts are defined in `package.json`.

## Commands

```bash
npm run format
```

Runs Prettier on Markdown, HTML, JS/TS, and JSON files.

```bash
npm run format:check
```

Checks formatting without writing changes.

{
  "name": "interdead-it",
  "version": "1.0.0",
  "description": "Landing entry point for the InterDead metaverse with Hugo, Workers, and shared cores.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "format": "prettier --write \"**/*.{md,html,js,ts,json}\"",
    "format:check": "prettier --check \"**/*.{md,html,js,ts,json}\""
  },
  "devDependencies": {
    "prettier": "^3.3.0"
  }
}
